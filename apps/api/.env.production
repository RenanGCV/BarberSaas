# Production environment variables
# These are placeholders - real values come from Railway environment variables

DATABASE_URL="${DATABASE_URL}"
JWT_SECRET="${JWT_SECRET}"
JWT_REFRESH_SECRET="${JWT_REFRESH_SECRET}"
PORT="${PORT:-3333}"
NODE_ENV="production"
